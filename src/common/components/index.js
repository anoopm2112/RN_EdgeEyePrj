import {
    Text, Container, Content, Button, Item, Input, Header, Label, Title,
    Tab, Tabs, TabHeading, Icon, CheckBox
} from 'native-base';

import CustomHeader from './CustomHeader';
import CustomIcon from './Icons';
import Card from './Card';
import ButtonField from './ButtonField';
import InputField from './InputField';

export { 
    Text, Container, Content, Button, Item, Input, Icon, Header, Label,Tab, Tabs, TabHeading, 
    Title, CheckBox, CustomIcon, Card, CustomHeader, 
    ButtonField, InputField
};